/*
import {useState} from "react";
import {Button} from "react-bootstrap";

export default function useHandButtonIcon() {
    const [buttonText, setHandUp] = useState("Raise Hand Up");
    const up = "Raise Hand Up";
    const down = "Raise Hand Down";
    setIcon(buttonText === up ? down : up)

}*/
